var Models = require('../models/index');
var config = require('../../config/config');

var Dir = {};

/**
 * App Models
 * @type {[type]}
 */
Dir.Models = Models;

/**
 * Config File
 * @type {Object}
 */
Dir.config = config;

module.exports = Dir;
